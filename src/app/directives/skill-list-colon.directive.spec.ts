import { SkillListColonDirective } from './skill-list-colon.directive';

describe('SkillListColonDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillListColonDirective();
    expect(directive).toBeTruthy();
  });
});

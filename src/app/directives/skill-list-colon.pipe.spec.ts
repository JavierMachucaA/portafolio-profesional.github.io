import { SkillListColonPipe } from './skill-list-colon.pipe';

describe('SkillListColonPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillListColonPipe();
    expect(pipe).toBeTruthy();
  });
});
